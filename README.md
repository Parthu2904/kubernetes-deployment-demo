# kubernetes-deployment-demo
# ☸️ Spring Boot on Kubernetes

A minimal Kubernetes deployment for a Spring Boot microservice.

### ⚙️ Includes
- Deployment  
- Service (LoadBalancer)  
- Health probes  

### 🧠 Commands
```bash
kubectl apply -f deployment.yaml
kubectl get pods
kubectl logs <pod-name>
